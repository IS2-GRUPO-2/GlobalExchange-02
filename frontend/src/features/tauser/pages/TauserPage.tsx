import TauserInicio from "../components/TauserInicio";

/**
 * Página para finalizar las operaciones de compra/venta.
 * Permite a los usuarios a acceder a las terminales de autoservicio.
 */
export default function TauserPage() {
  return (
    <main className="min-h-screen bg-gray-50 py-8">
      <TauserInicio />
    </main>
  );
}